rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 팀원이 로그인/인증 여부와 상관없이 자유롭게 전술, 선수, 회비, 경기 일정을 
    // 조회하고 공동으로 편집(추가/수정/삭제)할 수 있도록 공개 규칙을 적용합니다.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
